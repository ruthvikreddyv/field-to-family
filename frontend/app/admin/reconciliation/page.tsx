'use client';
import { useState } from 'react';
import { api,money } from '@/lib/api';
export default function Reconciliation(){const [date,setDate]=useState('');const [d,setD]=useState<any>(null);const load=()=>date&&api<any>(`/admin/reconciliation?delivery_date=${date}`).then(setD);return <div><div className="eyebrow">Cash control</div><h1 className="h2">Cash reconciliation</h1><div className="inline-actions card card-pad" style={{marginBottom:16}}><input type="date" value={date} onChange={e=>setDate(e.target.value)} className="btn btn-outline"/><button className="btn btn-primary" onClick={load}>Load</button></div>{d&&<><div className="grid grid-3">{d.partners.map((p:any)=><div className="metric" key={p.partner}><div className="muted small">{p.partner}</div><div className="big">{money(p.collected)}</div><div className="small muted">Expected {money(p.expected)} · Difference {money(p.difference)}</div></div>)}</div><div className="table-wrap" style={{marginTop:18}}><table className="table"><thead><tr><th>Order</th><th>Partner</th><th>Expected</th><th>Collected</th><th>Difference</th><th>Status</th></tr></thead><tbody>{d.orders.map((o:any)=><tr key={o.order_id}><td>{o.order_id}</td><td>{o.partner}</td><td>{money(o.expected)}</td><td>{money(o.collected)}</td><td>{money(o.difference)}</td><td>{o.payment_status}</td></tr>)}</tbody></table></div></>}</div>}
