'use client';
import { useEffect,useState } from 'react';
import { api,money } from '@/lib/api';
export default function Expenses(){const today=new Date().toISOString().slice(0,10);const blank={expense_date:today,category:'Transport',description:'',amount:'',payment_method:'CASH',reference:''};const [rows,setRows]=useState<any[]>([]);const [f,setF]=useState(blank);const load=()=>api<any[]>('/admin/expenses').then(setRows);useEffect(()=>{load()},[]);const add=async()=>{await api('/admin/expenses',{method:'POST',body:JSON.stringify({...f,amount:Number(f.amount)})});setF(blank);load()};return <div><div className="eyebrow">Operating costs</div><h1 className="h2">Expenses</h1><div className="card card-pad" style={{marginBottom:16}}><div className="form-grid">{[['expense_date','Date'],['category','Category'],['description','Description'],['amount','Amount'],['payment_method','Payment Method'],['reference','Reference']].map(([k,l])=><div className="field" key={k}><label>{l}</label><input value={(f as any)[k]} onChange={e=>setF({...f,[k]:e.target.value})}/></div>)}</div><button className="btn btn-primary" style={{marginTop:15}} onClick={add}>Record expense</button></div><div className="table-wrap"><table className="table"><thead><tr><th>Date</th><th>Category</th><th>Description</th><th>Amount</th><th>Method</th><th>Reference</th></tr></thead><tbody>{rows.map(e=><tr key={e.id}><td>{e.expense_date}</td><td>{e.category}</td><td>{e.description}</td><td>{money(e.amount)}</td><td>{e.payment_method}</td><td>{e.reference}</td></tr>)}</tbody></table></div></div>}
