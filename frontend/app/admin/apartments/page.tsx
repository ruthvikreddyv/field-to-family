'use client';
import { useEffect,useState } from 'react';
import { api } from '@/lib/api';
export default function Apartments(){const blank={name:'',address:'',coordinator_name:'',coordinator_phone:'',active:true};const [rows,setRows]=useState<any[]>([]);const [f,setF]=useState(blank);const load=()=>api<any[]>('/admin/apartments').then(setRows);useEffect(()=>{load()},[]);const add=async()=>{await api('/admin/apartments',{method:'POST',body:JSON.stringify(f)});setF(blank);load()};return <div><div className="eyebrow">Service area</div><h1 className="h2">Apartments</h1><div className="card card-pad" style={{marginBottom:16}}><div className="form-grid">{[['name','Apartment Name'],['address','Address'],['coordinator_name','Coordinator Name'],['coordinator_phone','Coordinator Phone']].map(([k,l])=><div className="field" key={k}><label>{l}</label><input value={(f as any)[k]} onChange={e=>setF({...f,[k]:e.target.value})}/></div>)}</div><button className="btn btn-primary" style={{marginTop:15}} onClick={add}>Add apartment</button></div><div className="table-wrap"><table className="table"><thead><tr><th>Name</th><th>Address</th><th>Coordinator</th><th>Phone</th><th>Active</th></tr></thead><tbody>{rows.map(a=><tr key={a.id}><td>{a.name}</td><td>{a.address}</td><td>{a.coordinator_name}</td><td>{a.coordinator_phone}</td><td>{String(a.active)}</td></tr>)}</tbody></table></div></div>}
