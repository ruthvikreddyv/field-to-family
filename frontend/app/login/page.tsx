'use client';
import { useState } from 'react';
import { useRouter } from 'next/navigation';
import { api } from '@/lib/api';
export default function Login(){const r=useRouter();const [mode,setMode]=useState<'login'|'register'>('login');const [f,setF]=useState({name:'',email:'',phone:'',password:'',flat_number:''});const [err,setErr]=useState('');const submit=async()=>{setErr('');try{const u=await api<any>(`/auth/${mode==='login'?'login':'register'}`,{method:'POST',body:JSON.stringify(mode==='login'?{email:f.email,password:f.password}:{...f,apartment_id:null})});r.push(u.role==='ADMIN'?'/admin':u.role==='DELIVERY_PARTNER'?'/delivery':'/account')}catch(e:any){setErr(e.message)}};return <main className="auth-wrap"><div className="card auth-card"><div className="eyebrow">F2F account</div><h1 className="h2">{mode==='login'?'Welcome back.':'Create your F2F account.'}</h1><div className="inline-actions" style={{marginBottom:16}}><button className={`btn ${mode==='login'?'btn-primary':'btn-outline'}`} onClick={()=>setMode('login')}>Login</button><button className={`btn ${mode==='register'?'btn-primary':'btn-outline'}`} onClick={()=>setMode('register')}>Register</button></div>{mode==='register'&&<div className="grid"><div className="field"><label>Name</label><input value={f.name} onChange={e=>setF({...f,name:e.target.value})}/></div><div className="field"><label>Phone</label><input value={f.phone} onChange={e=>setF({...f,phone:e.target.value})}/></div></div>}<div className="grid"><div className="field"><label>Email</label><input type="email" value={f.email} onChange={e=>setF({...f,email:e.target.value})}/></div><div className="field"><label>Password</label><input type="password" value={f.password} onChange={e=>setF({...f,password:e.target.value})}/></div></div>{err&&<p className="error">{err}</p>}<button className="btn btn-primary" style={{width:'100%',marginTop:15}} onClick={submit}>{mode==='login'?'Login':'Create account'}</button></div></main>}
