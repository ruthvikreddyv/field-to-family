'use client';
import { useEffect,useState } from 'react';
import Link from 'next/link';
import { api,money } from '@/lib/api';
import { useCart } from '@/components/cart-context';
import { Order } from '@/lib/types';
export default function Account(){const {addQuantity}=useCart();const [profile,setProfile]=useState<any>(null);const [orders,setOrders]=useState<Order[]>([]);const [err,setErr]=useState('');useEffect(()=>{Promise.all([api<any>('/auth/profile'),api<Order[]>('/customer/orders')]).then(([p,o])=>{setProfile(p);setOrders(o)}).catch(e=>setErr(e.message))},[]);if(err)return <main className="page"><div className="container empty"><p className="error">{err}</p><Link href="/login" className="btn btn-primary">Login</Link></div></main>;if(!profile)return <main className="page"><div className="container empty">Loading account…</div></main>;return <main className="page"><div className="container"><div className="eyebrow">Customer account</div><h1 className="h2">Hi, {profile.name}.</h1><div className="grid grid-2"><div className="card card-pad"><h3>Profile</h3><p><b>Email</b><br/>{profile.email}</p><p><b>Phone</b><br/>{profile.phone}</p><p><b>Flat</b><br/>{profile.flat_number||'—'}</p></div><div className="card card-pad"><h3>Order history</h3>{!orders.length?<p className="muted">No orders yet.</p>:orders.slice(0,8).map(o=><div className="row" key={o.order_id}><span><b>{o.order_id}</b><br/><span className="small muted">{o.order_status}</span></span><span><b>{money(o.total)}</b><br/><Link className="small" href={`/track?order_id=${o.order_id}`}>Track</Link><button className="btn btn-secondary small" style={{padding:'5px 8px',marginLeft:6}} onClick={async()=>{const ps=await api<any[]>('/products');for(const i of o.items){const p=ps.find(x=>x.id===i.product_id);if(p)addQuantity(p,Number(i.quantity))}}}>Reorder</button></span></div>)}</div></div></div></main>}
